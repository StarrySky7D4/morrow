# Morrow 技术路线、未来规划与开发指导方案

**执行基线建议稿 v1.0 | 2026-09-15**

**适用对象：** Morrow 主应用、可信 Rust 核心、插件 SDK、执行后端、平台适配、审计与重放工具，以及独立的 MorrowCloud 插件接入。

**核对基线：** `main@0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7`；应用 `0.1.9-test.50+55`；新增原生网络节点组件为 `test.51`。本文核对了仓库入口、SDK、路线与相关契约，没有重新构建、运行或提交代码。[R1][R2][R3][R8]

> 保留已有成果，以证据补齐关键链路；不再以新增模块数量代替系统完成度。

本文区分三类陈述：**已确认约束**来自本轮讨论；**仓库事实**以固定提交和引用为准；**推荐方案**是后续实施与验收建议。接口名、工作包编号和目标均属于方案，不冒充现有 API 或已通过的测试。

**核心方向：** 全平台 Flutter 界面 + 可移植 Rust 核心 + 平台能力适配 + 可替换插件执行后端。卡片保存内容，插件提供能力，瀑布流组织呈现。运行期以 Cap’n Proto 为契约；自有持久化与外部业务交换使用 Protobuf + LZ4。

**交付目标：** 先贯通授权、固定输入、执行、提交、封存、验证与隔离重放；同时推进平台可行性和 i18n，最后依据声明的能力范围完成产品与 SDK 发布评审。

---

# 阅读导航与决策摘要

## 建议阅读顺序

产品与总体决策阅读第 1、2、12、17 章；核心与 SDK 开发阅读第 3–8 章；UI、平台与本地化开发阅读第 8–10 章；迭代执行与代码审查阅读第 12–16 章。来源编号见文末，仓库引用均固定到本次基线。

| 章节 | 主题 | 主要输出 |
| --- | --- | --- |
| 1–2 | 起点、约束、架构及组件归属 | 不重写什么、核心控制什么 |
| 3–4 | 协议、存储、事务及迁移 | 数据如何交换、保存与恢复 |
| 5–6 | SDK、权限、执行域、线程与依赖 | 插件如何接入并被限制 |
| 7–8 | 审计重放与核心 i18n | 如何核对历史、支持多语言 |
| 9–11 | 瀑布流、全平台、IO 与 Cloud | 能力如何转化为真实产品 |
| 12–14 | M0–M7、实施波次、首批工作包 | 先做什么、如何判定完成 |
| 15–17 | 验证、开发规则、风险与长期规划 | 如何持续演进而不失控 |

## 五项立即生效的实施建议

**第一，保留现有 M0–M7 编号。** 新增 i18n、原生执行域和完整 IO 是对原有门槛的补充，不以新路线清零历史成果，也不降低 0.2.0 的退出要求。[R3]

**第二，先解决身份与语义，后翻译文案。** 中文阶段名仍参与业务分派；先引入稳定业务标识和显式兼容，再添加本地化呈现。[R6]

**第三，默认延续已有受限 Wasm 路线。** 原生 runner 作为独立执行配置补充，不把普通第三方库加载进可信核心进程。[R4]

**第四，系统接口统一策略而非统一语言。** Rust 管权限、提交和审计；文件选择、系统分享、窗口和媒体呈现可复用经过审核的 Flutter／原生平台实现。

**第五，每次新增功能附带失败证据。** 不允许只演示成功路径；至少包含拒绝、冲突、取消、未知结果和重启核对中与本次变更相关的场景。

---

# 1. 当前起点与必须保留的约束

## 1.1 仓库现状

| 领域 | 本次可确认状态 | 本方案的处理 |
| --- | --- | --- |
| 应用与核心 | 应用 test.50+55；Windows 已走 Rust 宿主、受限 Wasm 业务及格式 14 内容库 | 保留主路径，补验收，不回退为全量重写 |
| 第三方 SDK | C11、C++17、Rust；guest ABI2 / runtime7 / task3 / UI1 / dependency-call1 | 保留冻结原包；新能力独立版本演进 |
| 插件管理 | 已有导入、批准、启停、版本检查、卸载和基础 UI | 补必需依赖管理、长期任务和通用内容能力 |
| 网络 | test.51 是受信任原生双向传输原型 | 不把它等同于正式 guest 网络授权与审计 |
| 全平台 | Web／Android 主应用尚保留旧路径 | 各平台早建探针，独立证明接入与恢复 |
| 本地化 | 页面存在中文业务分支；UI1 主要承载字面文本 | i18n 纳入契约与迁移阶段，不仅增加翻译表 |

以上结论依据应用清单、当前路线、SDK 和原生网络说明；测试报告不等于本次独立复测。[R2][R3][R4][R6][R7][R8]

## 1.2 已确认架构约束

卡片、工作区和视图配置分开；插件不能拥有用户数据。禁用或卸载插件后，基础信息、未知字段、已有预览和原始附件仍可保留与导出。

MorrowCloud 是独立项目及可选普通插件。本地创建、编辑、恢复、安装插件和验证日志不依赖云账号；也不能把 Cloud 作为唯一的审计见证。

内部运行期采用预编译 Cap’n Proto；原生 IPC 以受控 mmap 为主。Flutter／Rust／Wasm 跨界可以复用该契约；普通进程内调用不强行序列化。

所有自有持久化和外部业务交换采用 Protobuf + LZ4，包含内容、配置、插件状态、日志、缓存、快照、证据、导出与网络。JSON 仅留外围兼容，不充当转换中间层或权威存储。

全平台是内容兼容和能力分级，不承诺普通浏览器拥有全部原生接口。实现不可用或保证不足时明确拒绝，不静默外发、不隐式使用远端节点。

## 1.3 仍需验证的选型

保留现有已验证数据库和执行器路径。是否替换运行时、增加 native-runner、使用哪一种 i18n 消息引擎、桥接工具和线程池，均以兼容、故障测试和实际性能决定。不得仅为统一版本或追求最新而升级整套依赖。

---

# 2. 目标架构与现有组件管理

## 2.1 五层职责

| 层次 | 负责 | 禁止形成的依赖 |
| --- | --- | --- |
| Flutter 呈现 | 页面、输入、焦点、瀑布流、主题、可访问性和本地化缓存 | 不另存正式内容；不直接授予插件权限 |
| Rust 可信核心 | 内容事务、身份、权限、契约、持久化、任务协调和审计 | 不认识特定 Cloud 品牌；不依赖 Widget |
| 平台后端 | 存储、凭据、资源选择、映射、进程、Worker 与媒体 | 不在平台层复制卡片业务规则 |
| 插件执行 | Wasm、受控原生 runner、受批准的可信内置模块 | 不直接持有资料库或审计私钥 |
| 工具与资源 | SDK、验证器、重放器、语言包、模板、兼容夹具 | 不成为旁路授权或第二个权威来源 |

建议保持现有 core/、plugin_runtime/、workbench_host/、sdk/、audit/、core-web/ 等结构，先改善内部依赖边界；本阶段不为目录美观大规模搬迁。新增本地化和平台接口可以先作为模块，不强制每项成为独立 crate、仓库或服务。

## 2.2 组件归属

| 现有或目标能力 | 建议归属 | 管理策略 |
| --- | --- | --- |
| 卡片、附件、关系、恢复和事务 | 核心 | 不允许通过禁用插件失去数据访问 |
| 窗口、玻璃、主题、拖动和焦点 | UI／平台基础设施 | 复用成熟实现，开放参数而非任意接管 |
| Markdown 基础阅读及未知卡片预览 | 随包基础能力 | 保留救援与导出入口 |
| 项目、实验、专业转换 | 业务扩展 | 使用公共类型注册和内容命令 |
| 播放列表、歌词、音乐面板 | 可选音乐插件 | 共享宿主媒体会话，不各建无限线程池 |
| 文件选择／系统分享／剪贴板入口 | 平台适配 | 返回受控引用，后续操作重新检查范围 |
| i18n 引擎、语言策略与消息身份 | 核心能力 | 语言包是数据扩展，不需执行线程 |
| Cloud、联网插件、API 服务 | 可选业务与服务扩展 | 出站、监听、发布、凭据使用分别授权 |

## 2.3 界面与核心之间的调用原则

Flutter 保留临时输入和视图缓存；正式修改由核心返回回执。FFI 或现有子进程桥只承载少量稳定入口；结构化批量请求使用 Cap’n Proto，避免逐字段跨界。

不在 build()、布局或输入事件关键路径中执行压缩、证据落盘、网络等待或大型查询。Rust 入口负责快速接纳或拒绝，重任务异步执行。复用平台插件不等于复制其权限模型；它必须被包在 Morrow 的可信能力适配层内。

---

# 3. 协议、二进制封装与兼容策略

## 3.1 两套编码、统一语义

| 用途 | 契约 | 约束 |
| --- | --- | --- |
| 调用、任务、依赖、UI、语言上下文 | Cap’n Proto | 构建期生成，运行期仍校验范围与关联 |
| 原生大对象交付 | Cap’n Proto 描述 + mmap | 描述不授予访问权；不直接暴露整库映射 |
| 正式记录、语言包、状态、审计和证据 | Protobuf + LZ4 | 原件和模式版本明确；不整体压缩成巨型数据库对象 |
| 自有外部接口 | Protobuf + LZ4 | 与存储契约分别演进；不导出内部句柄 |
| 用户附件／旧原件／第三方响应 | 原始字节 + 类型化描述 | 不误译、不重编码证据；兼容适配留在边缘 |

统一编码不替代记录分块、事务、索引或备份设计。小记录采用有界容器，日志按段、附件按块；不可压缩材料可以在已约定容器中明确使用未压缩块。不得无版本改变现有 LZ4 封装。

## 3.2 协议演进

保留 guest-v1-rc1 原件及现有 schema 摘要。当前加载器对契约文本摘要有精确匹配规则，因此“只改注释”也不能随意刷新基线。新 i18n、异步 IO 或原生 ABI 以独立版本和路由接入，旧插件继续使用旧路径。[R5]

Cap’n Proto 允许满足规则的追加式演进，但这不自动绕过 Morrow 自己的摘要门禁。不得复用字段编号，未知特权指令拒绝，未知用户内容按原格式保留；类型 ID、包摘要和权限句柄不能互相替代。[W2]

构建必须固定编译器、SDK、模板和产物摘要。保留两组测试：当前源码编译测试与旧二进制执行测试。严禁为通过兼容门禁重编译冻结 guest 或替换黄金样本。

## 3.3 摘要和身份

区分 wireDigest、证据摘要和稳定业务 ID。Protobuf 不保证跨构建、跨版本的规范化序列化；签名针对实际保存的固定字节，验证不得先解码再重编码。业务去重使用显式操作 ID 与已定义的命令匹配规则。[W3]

数据转换必须测试字段存在性、未知字段、未知枚举、UInt64、字符编码与错误分类。内部 capability、原生地址、mmap 偏移不得当作网络或持久化权限恢复。

---

# 4. 内容、可靠存储、迁移与长期容量

## 4.1 一份内容权威

Card 保存内容身份、类型、版本、修订、正文、附件和关联；Workspace／ViewPlacement 保存组织与呈现；Draft 保存未正式提交的编辑，不反向覆盖正式内容。标题、类型标识和阶段标识与显示语言无关。

所有写入进入统一 ContentGateway。插件和 UI 提交有限命令，包含 operationId 与 expectedRevision；核心检查实际连接授权、当前修订、字段预算和附件引用，再进入短事务。不得在事务中等待网络、插件或用户输入。

## 4.2 提交与恢复顺序

固定输入及必要证据 → 记录受控意图 → 开启短事务 → 再次授权与修订校验 → 写内容、回执、证据引用及待封存事件 → 提交 → 幂等封存 → 返回实际达到的状态。

本地提交、审计封存和独立见证分开表达。事务提交后响应丢失，通过原 operationId 查询；同一 ID 不同命令拒绝。后续故障不抹掉此前事实，也不能触发自动重复的外部操作。

## 4.3 旧数据迁移

先保留原资料并验证可恢复副本，再在独立目标中迁移。含中文枚举的旧业务类型由版本化适配器解释；新记录使用稳定标识。遇到不明值保留原值或转为可读兼容状态，不猜测转换。

已签名记录、历史命令及插件原件不重写。迁移形成新操作与来源关系，验证用户内容、附件、未知字段、关系和回执。程序版本回退不等于数据回退；不能恢复旧整库而无声覆盖用户后续编辑。

## 4.4 容量是功能，不是报错补丁

分别管理活动内容、附件、未提交暂存、已发布证据、审计段、索引和 WAL 等费用。物理磁盘用量与逻辑配额分开报告；签名不阻止垃圾回收设计，但删除必须有明确的材料状态和引用规则。

首要补齐已发布证据的可审计退休、元数据增长策略、磁盘不足下的终态记录预算及安全维护入口。清理不得删除永久操作身份或伪造完整重放状态；旧快照与映射仍持有材料时，不宣称已经物理擦除。

备份验证采用“恢复到新目录 → 校验身份和内容 → 明确激活”，不能只检验压缩包能打开。恢复旧内容快照不得自动恢复已撤销的插件权限或旧执行句柄。

---

# 5. 插件 SDK、权限和生命周期

## 5.1 一套业务 SDK，多个入口适配

| 后端 | SDK 入口 | 默认信任策略 |
| --- | --- | --- |
| Wasm | 固定导入／导出 + 线性内存适配 | 延续受限默认路线，逐项批准宿主能力 |
| 原生独立程序 | Cap’n Proto IPC 桩 | 必须有实际平台隔离，不以普通子进程代替沙箱 |
| 原生库由 runner 加载 | 版本化 C ABI 函数表 | 优先于第三方库直接进入核心进程 |
| 可信进程内模块 | 受审核内部 API／C ABI | 明确属于可信代码范围，不声称独立沙箱 |

当前正式开发入口以受限 Wasm 为主，本地回调 ABI 不能直接被宣传为已稳定的原生加载协议。[R4][R5]

业务代码复用类型化请求、任务、依赖与 UI 描述。新原生入口只补 create/invoke/stop/destroy 等适配，不另建权限和存储体系。跨 ABI 不传 Rust Vec、String、trait object 或 C++ STL 对象；分配、释放和借用期限必须明确。

## 5.2 权限强制执行

有效权限来自声明上限、用户批准、工作区策略、当前实例和本次委托的共同约束。查询、缩略图、变更日志、资源名称和日志读取也要检查范围。创建卡片不赋予插件永久所有权。

宿主以真实通道绑定身份；实例代次、服务绑定代次及短期句柄不可由插件自报。依赖调用不继承提供者的全部权限；外发、监听、发布、凭据使用和本地文件修改分开授权。

撤权先阻止新操作，在提交或交付前再次检查。已经被插件读取的数据无法被要求“忘记”，已经发送的内容也无法由本地撤权自动收回；严格边界依靠最小交付和独立执行域。

## 5.3 生命周期与故障

安装校验不执行插件；注册只读取静态声明。准备时先设置限额和能力，再执行获准初始化；未就绪服务不接业务。退出按拒绝新任务、排空、撤权、取消、释放和终止推进。

取消、超时、trap、业务失败和已提交结果分别表示。旧实例迟到结果不得产生新写入。自动恢复有限且不重放业务副作用；连续失败隔离，安全模式仍可访问基础内容和导出。

插件 API 不是恶意原生代码的安全边界。原生旁路必须由系统隔离限制；做不到的平台明确标为高信任配置，不把 SDK 约定当作强制保证。

---

# 6. native-runner、线程与依赖协作

## 6.1 以执行域为强边界

一份 runner 程序可以启动多个进程实例；每个进程承载一个明确 ExecutionDomain。可以共驻的原生组件必须接受共享地址空间、权限信任与共同重启风险。线程是调度单位，不是原生内存安全边界。[W5]

包内轻量库在调用线程执行，不需要单独线程。库的可变状态按调用者或实例隔离；使用进程全局状态、全局 locale 或自带复杂线程池的库，优先独占域。跨信任域依赖以服务接口调用，不通过函数指针互通。

## 6.2 调度策略

| 策略 | 使用场景 | 实施要求 |
| --- | --- | --- |
| Inline | 短小、可重入库调用 | 继承根任务的期限、预算和上下文 |
| Serial | 有状态、不要求固定线程 | 每实例队列，复用线程池 |
| Parallel | 纯计算或明确支持并发 | 同时限制请求并发与每任务并行 |
| ThreadAffine | 特定线程创建／使用／销毁 | 固定线程通道，关闭时核对回调 |
| LibraryManaged | 自带并行运行库 | 单独批准，避免外层与内层同时满核 |

全局调度分配计算许可，各 runner 维护自己的有界执行器。线程数、CPU、堆、共享对象、宿主代办解码与网络预算分别计量。SDK 细粒度记账用于合作代码，操作系统域级限制负责兜底。

## 6.3 依赖与死锁

必需依赖与可选依赖分别声明，绑定精确接口、提供者产物和批准版本。强启动依赖拒环；运行期等待环与事件循环另行检测。按 capability 选择提供者，不能因安装了更高版本而静默换服务或数据去向。

短库直接调用；并行子任务共用根预算；跨域调用优先异步，不占住所有计算线程等待同池子任务。取消和剩余期限沿链传递，子调用不能重新获取无限预算。保留旧同步 ABI，新异步语义另行版本化。

## 6.4 停止与升级

不能靠强杀某条原生线程恢复共享进程。无法可靠排空时，撤销该域操作权限并终止整个 runner；同域其他组件受到影响必须在合并前接受。

库卸载需确认活跃调用、任务、回调和依赖其析构代码的对象均释放。首期采用排空旧域、建立新域、重新绑定，不做复杂热卸载。不可信域结束后不清空对象即复用为新的敏感信任域。

---

# 7. 审计、证据、验证与重放

## 7.1 保证分层

**插件侧不可修改**是权限隔离目标；**篡改可检测**是固定原件、哈希链和签名目标；**回滚可检测**需要适用范围内较新的独立检查点。完全控制宿主、私钥和全部副本的攻击者不在单机软件绝对保证范围内。

记录标记 HostObserved 与 PluginReported。签名证明宿主记录了什么，不证明插件结论正确，更不证明存在旁路时所有操作都已经记录。

## 7.2 三类记录

| 类别 | 权威来源 | 保存原则 |
| --- | --- | --- |
| 审计账本 | 宿主 | 原始 Protobuf 字节固定后建立保护，再 LZ4 封装 |
| 诊断日志 | 宿主或插件 | 结构化、脱敏、限流，丢弃数量可解释 |
| 重放证据 | 宿主协调 | 复用不可变对象，保留产物、输入和所需外部观察 |

同一链路绑定 rootOperationId、实例与服务代次、接口／模板摘要、授权快照、输入输出证据和提交状态。稳定 ID 与原始序列是依据，墙上时间不能单独定义顺序。

已签 Protobuf 原件不得解码重编码后验签；派生索引可重建，但不能成为另一份审计事实。[W3] i18n 不改写历史事件码，精确文案复现另记录资源摘要与上下文。

## 7.3 重放能力

状态重建覆盖已提交修改；确定性重放先覆盖纯计算；外部效果采用录制响应，默认断开真实网络、生产数据库和系统写接口。支持 Wasm 不自动等于跨平台逐位确定，需控制宿主导入、浮点行为和中断配置。[W4]

重放固定实际二进制与依赖，不仅是版本标签。原生多平台变体不同，跨后端运行称为一致性测试，不冒充历史环境。证据清理后标记 MaterialsRetired / ReplayUnavailable，保留合法清理事件。

## 7.4 必须补齐的系统链

把授权、交付、依赖调用、执行故障、内容投影、正式提交和封存贯通，而不仅保存单个转换函数。独立验证器不加载插件、不登录 Cloud；重放器只在隔离副本执行。

审计无法接纳时阻止必须留证的新操作；为已有任务收尾和维护清理预留预算。不要把每帧布局和鼠标高光逐条写入永久账本，也不要为降低开销跳过成功的安全相关操作。

---

# 8. 核心 i18n：提前建设，分层落地

## 8.1 第一项改造不是翻译

当前 workspace_pages.dart 以中文页面和阶段字符串分派，且将 stage 文本传入命令；UI1 节点的 label/text 是字面文本。[R6][R7] 应先分离业务标识、消息标识与显示文案，再增加语言。

业务阶段由对应类型／业务扩展定义稳定 ID，核心只维护通用身份规则。不得让核心枚举所有插件的中文业务状态。旧格式保留原字节，版本适配器将其解释成新模型；迁移不得重写已签历史。

## 8.2 上下文与资源

建议提供不可变 LocaleContext：uiLocale、formatLocale、timeZone、resourceGeneration；语言影响业务输出时再绑定 contentLanguage、outputLocale、collationProfile 与引擎版本。它随任务而不是线程流动，不能修改进程全局 setlocale 来切换插件语言。

语言匹配参考 BCP 47／CLDR，显式区别文字体系、地区、格式和时区；不把所有 zh 标签合并，不用机械截断代替全部匹配规则。[W6]

正式语言包使用 Protobuf + LZ4，运行期消息引用使用 Cap’n Proto。建议源资源采用经验证的现成格式，Fluent／ICU4X 等作为候选；先验证复数、具名参数和原生／Wasm 一致性，再锁定方案，不自创完整消息语言。

## 8.3 SDK 与 Flutter

新 UiText 支持 LiteralText 与 MessageRef(namespace, id, revision, typedArgs)。用户正文保留原文；核心错误、权限提示使用宿主命名空间，插件不能覆盖或冒充。参数不作为模板、HTML 或代码再次解释。

通过独立 i18n / UI 契约版本接入，保留 UI1 路径及冻结原包。格式本身可以追加演进，不代表可以跳过本仓库精确 schema 摘要检查。[R5]

Flutter 复用 Localizations、Material／Cupertino／Widgets delegate 和 supportedLocales；应用自有资源通过自定义适配读取。每次 build 不同步跨界取词，采用只读缓存和批量准备；框架提供上述本地化机制。[W1]

## 8.4 首次交付与验收

建议先覆盖简体中文和英文，并建立长文案与 RTL 伪本地化测试。正式启用其他语言需翻译审校、字体、布局和平台元数据一起验收。

切换语言只刷新呈现，不清空 IME 草稿、焦点、滚动锚点、内容修订和正在运行任务的输出语言。权限文案由宿主产生且保留可核对版本。离线缺词、损坏包、宿主故障时仍有最小恢复文案。

搜索排序与界面语言分开；字节限额、UTF-16 索引与用户字符计数分别定义。审计按当前语言展示不改签名；精确历史文案或报告重放固定资源与格式化规则版本。

---

# 9. 卡片工作台、瀑布流与体验路线

## 9.1 从底座能力回到用户工作流

优先完成“收集 → 整理 → 关联项目 → 实验／处理 → 找回与复用”，而不是继续增加固定页面和材质选项。允许想法只收藏或搁置，不强迫所有内容变成任务。

一张卡多视图引用，基础预览不依赖插件在线。跨工作区移动、引用与复制必须是不同操作。编辑器有持久草稿，插件故障或切换语言不能清空用户尚未保存的工作。

## 9.2 真正的虚拟化瀑布流

Flutter 按视口与有限预取范围构建卡片。核心分页查询不等于 UI 虚拟化，不能把全量 Column / Wrap 当成已解决规模问题。Flutter 官方也建议大型列表使用惰性构建并避免昂贵的全量布局。[W7]

建议将卡片呈现分成摘要、缓存预览和完整详情。缓存键包含内容修订、渲染器版本、语言／格式上下文、宽度和主题等实际影响因素。图片加载、语言切换和同步更新不能任意改变用户当前阅读锚点。

相同平台能力下，不因为卡片多就创建同等数量的进程、运行时或播放器。不可见视图释放临时订阅，明确授权的音乐和持续任务保留自己的生命周期。

## 9.3 插件 UI 与系统交互

宿主绘制统一外壳、状态、错误和权限对话框。声明式节点有数量、层级、文本、资源来源和更新频率上限；首期覆盖表单、表格、预览和简单控制，不发展为无限通用 UI 编程语言。

文件选择、系统分享、输入法、窗口和媒体纹理可以复用 Flutter 平台能力。实际系统调用结果进入 Rust 管理的任务，不让第三方 Morrow 插件直接操作平台 MethodChannel。

媒体控制走协议，帧走经过管理的纹理／媒体数据通道；不为每帧先编码成消息再解码。玻璃效果设可读性和低资源模式，不先重写 Flutter 渲染器。

## 9.4 体验验收

以 100 / 1,000 / 10,000 张混合内容卡为测试规模，不作为已承诺容量。覆盖连续滚动、长文本编辑、10 次语言切换、插件停用、后台处理和重启恢复。

记录帧耗时、加载时延、缓存命中、内存峰值和后台资源，不仅展示截图。测试必须包括触摸、键盘、屏幕阅读与 RTL；平台不具备某个交互时提供等价入口，不将能力差异隐藏为失效按钮。

---

# 10. 全平台实现与能力验收

## 10.1 统一契约，分别适配

| 平台 | 建议实现路线 | 提前验证的限制 |
| --- | --- | --- |
| Windows | Flutter + 原生 Rust + 独立受控 runner | ACL、进程树、映射、密钥、打包与恢复 |
| macOS | Flutter + 原生 Rust + 平台桥 | 沙箱、签名／公证、插件执行与商店配置 |
| Linux | Flutter + 原生 Rust + 原生／portal 适配 | 发行版、ABI、桌面会话、沙箱包和凭据后端 |
| Android | Flutter + Rust .so + 薄 Kotlin/JNI | URI 资源、进程服务、后台终止与重开 |
| iOS／iPadOS | Flutter + 随包可信 Rust + 已验证执行后端 | 插件分发、可执行内存、后台和密钥能力 |
| Web／PWA | Flutter Web + Rust/Wasm + 可信 Worker | 浏览器 IO、OPFS、异步 guest、配额及生命周期 |

这是一张实施与验证表，不是对现有仓库的支持声明。M7 按实际发布配置、系统版本、架构和浏览器分别验收。不能只凭 Rust 编译通过就宣布 Flutter 应用和插件可用。

## 10.2 能力报告不是平台布尔值

建议每个能力描述接口版本、是否实现、授权条件、用户动作要求、任务期限、执行位置和保证等级。分别返回 UnsupportedPlatform、PermissionDenied、NeedsUserAction、BackendUnavailable、AssuranceMismatch。

普通 Web 不承诺原生接口全集；有界副本与可转移缓冲可以维持固定输入，SharedArrayBuffer 的共享可写语义及部署条件不能被当成原生只读 mmap。[W8] 缺失能力不自动使用 Cloud 或其他远端节点。

平台资料可共用原始数据与协议，但不能直接共享权限句柄、OS 路径或执行实例。恢复时重建授权和资源引用；系统选择器返回的 URI／安全作用域引用不应被简化为永远有效的字符串路径。

## 10.3 技术资格与渠道资格分开

Apple 与 Google Play 对动态代码和插件分发有规则；解释执行或 Wasm 标签不自动证明可发布。建立单独的渠道验证任务，按实际方案准备可运行样例和说明，不为通过渠道把 Cloud 写死进核心。[W9][W10]

支持级别建议分别标明：编译通过、核心集成通过、产品端到端通过、渠道发布通过。读取、编辑、插件执行、后台、审计保护和重放各自列证据，不合并成一项“全功能支持”。

从 M1 起并行维护所有平台的最小探针。生产验收可分批，但不能把全平台可行性调查推迟到 Windows 架构完全固化之后。

---

# 11. 文件、网络 API 与 MorrowCloud

## 11.1 用现有网络原型补齐正式 IO，而不是另建旁路

当前 network_node 是受信任原生传输与固定纯转换适配；它不证明 guest 已具有通用网络 API 或完整发布授权。[R8] 后续把客户端与入站分派接入真实实例、资源范围、审计和结果恢复。

首个有外部效果的请求就应实现持久意图、operationId、期限、取消和 Unknown 核对。不得先开放任意写请求，再将恢复与录制作为后补优化。

出站 origin／方法、入站监听、路由发布、远端主体、凭据与内容对象分别授权。入站身份不等于插件身份；依赖链每一条外部效果必须在原批准范围内，不能让远端选择任意 URL、包或资源。

## 11.2 IO 的分段路线

先固定 IO 类型、错误、异步任务和句柄语义；贯通选中文件分块读取、完整普通 HTTP 请求响应及可恢复外部写入。GET 仅作连通性探针，不是完整交付。

随后推进认证／多账号、分页限流、上传下载、流、SSE、WebSocket 和 API 节点能力，每项独立 profile 与预算。大数据不强行塞入旧 64 KiB 帧；旧同步 ABI 保留，新会话和分块协议显式演进。

不自动重试外部副作用；不以连接断开、取消或 CORS 错误证明远端未执行。TLS／认证不替代插件权限；LZ4 不提供密码学完整性。自有业务使用 Protobuf + LZ4，第三方既定格式仅在适配边缘存在。

## 11.3 MorrowCloud 接入规则

Cloud 插件通过公共接口处理账户连接、同步映射、变更游标和传输队列；不获取私有数据库连接或审计私钥。安装 Cloud、联网同步和远端执行不是同一个授权。

此前讨论的轻量云数据、近期富文本与非文本元数据策略，应保留为 Cloud 项目的参数与业务范围，不把其额度或同步策略固化到 Morrow 核心。不能因宿主提供附件接口就默认上传全库原件。

同步回来的内容不会自动安装、启用或授权插件；停用 Cloud 保留本地内容，远端仅存附件要明确提示。移除 Cloud 后，本地创作、备份、插件安装和独立日志验证继续可用。

Cloud 上线不作为 Morrow 本地 0.2.0 的前置条件；普通服务接口需有真实插件样例验证，不因 Cloud 暂未上线而跳过通用接口验收。[R3]

---

# 12. 沿用 M0–M7 的正式退出门槛

以下是对现有阶段的增补，不是重新编号或把已有阶段判定为从零开始。状态由证据决定，阶段号不映射到 test 序号或固定发布日期。[R3]

| 阶段 | 后续重点交付 | 退出门槛 |
| --- | --- | --- |
| M0 基线与验证 | 当前事实、约束追踪、威胁模型、平台与语言矩阵 | 每条约束有代码责任区、测试和未完成状态 |
| M1 内容与契约 | 语言无关标识、i18n、新 IO、原生 ABI 的版本化设计 | 旧／新／未知／畸形向量一致；旧 guest 不重编译通过 |
| M2 可靠提交 | 大内容、草稿、事件与附件一致性、迁移恢复 | 创建／修改／取消／重启核对可靠；原件与未知字段保真 |
| M3 授权与共享 | 正式 A→B、执行域、资源租约、依赖与撤权 | 固定输入一致；越权、旧代次、未释放页面不能产生新效果 |
| M4 审计恢复 | 全链路宿主事实、幂等封存、配额、合法材料退休 | 已提交不漏记；篡改可检；无法留证时正确阻断 |
| M5 隔离重放 | 依赖闭包、宿主投影、录制外部响应和独立工具 | A→B→提交→封存→核验→重放含故障矩阵通过 |
| M6 真实插件产品 | 公共 SDK、依赖管理、UI、i18n、迁移和瀑布流 | 外部开发者不靠私有入口；停用卸载不丢内容 |
| M7 平台与发布 | 各声明平台的系统、分发、恢复和兼容资格 | 能力矩阵对应端到端证据；Cloud 无特殊依赖 |

## 12.1 0.2.0 的评审规则

继续使用 0.1.9-test.x 推进。已有局部原型和测试不能替代 M3–M5 整条链；基础 guest 候选稳定不等于完整 SDK 稳定。只有汇总 M0–M7 对声明范围的证据后才进入 0.2.0 评审。[R3][R5]

全平台是持续目标；未验收平台明确为实验，不暗示一次发布已经全平台等价完成。原生进程内插件、独立原生 runner、复杂流式接口也各自有资格状态，不能因为某个后端通过就同时转为稳定。

## 12.2 不要降低历史门槛

i18n 不能以修改 schema 摘要而失去旧 guest 兼容；新网络能力不能绕开已有授权与证据规则；新 UI 不得启用第二份正式存储；迁移不删除难以解释的旧字段。

任何退出门槛的改变均形成架构决策记录，说明原约束、原因、影响、迁移和验证。不能因为交付压力将“暂缺”改成“设计允许”，也不能将所有实验功能永久绑定到同一个笼统的完成标志。

---

# 13. 下一轮实施波次与依赖关系

## 13.1 三条并行工作线

**主线 A：可信数据与执行。** 围绕现有 M2–M5 补齐真实共享对象、授权、依赖、提交、审计、清理和重放。

**主线 B：产品与 SDK。** i18n、公共内容能力、第三方安装／依赖 UI、草稿、迁移和瀑布流，全部消费主线 A 的接口。

**主线 C：平台与工程。** 持续验证非 Windows 平台、构建／签名、消息互操作、故障恢复、性能和发布证据，不等待主线结束。

个人开发或少量并行代理采用 WIP 限制：同时一个涉及核心不变量的合并任务，其他工作保持在接口明确的测试、UI 或平台探针范围。并行并不等于同时改同一批 Schema 和数据库格式。

## 13.2 推荐波次

| 波次 | 优先内容 | 进入下一波的条件 |
| --- | --- | --- |
| A：基线与本地化身份 | 当前版本清单、兼容门禁、业务 ID、消息与错误契约 | 新增代码不再依赖翻译值；旧原件仍可用 |
| B：补齐关键安全链 | 正式 A/B 通道、有限 IO、提交结果、全链路证据、容量收尾 | 真实程序通过撤权、崩溃、Unknown、重放测试 |
| C：真实插件与工作台 | 公共附件／正文 API、依赖管理、i18n UI、持久草稿、迁移、瀑布流 | 一个仓库外插件完成整个用户流程，不改宿主 |
| D：受控原生扩展 | C ABI、native-runner、执行域和线程策略 | 硬隔离和限额有实证；收益经测量值得引入 |
| E：平台资格与发布 | 桌面／移动／Web 各端的产品与渠道验证 | 按声明范围满足 M7，无隐含全平台承诺 |

波次不是固定工期。i18n 身份和版本化设计先开始；核心文本、恢复页和关键权限文案在 B/C 同步落地。原生 ABI 设计和平台可行性可提前探针，但不因此先开放不受控原生加载。

## 13.3 原生路线的加入条件

只有实际任务需要系统库或测量证明现有 Wasm 后端存在重要瓶颈，才提升对应原生后端优先级。先通过宿主原生能力服务解决可复用功能，再决定是否提供整插件原生变体。

已在默认 Windows 产品中集成的功能可继续维护，但不能将早期产品接入当作关键链路门槛已通过；大规模资料切换以恢复和兼容证据为前提。

---

# 14. 首批可执行工作包

以下编号属于本方案工作包，可映射到现有任务系统，不替换 M0–M7。每个任务只解决一组可验收问题；报告包含基线、修改范围、结果、失败证据与剩余限制。

| ID / 优先级 | 修改责任区 | 交付与验收 |
| --- | --- | --- |
| ROAD-01 / P0 | 工具与文档 | 由构建事实生成版本／能力清单；能定位应用、Schema、数据库、平台和来源提交 |
| ROAD-02 / P0 | core/schemas、sdk/contracts | 新消息与本地化契约草案及兼容路由；冻结旧包原样运行，新畸形向量拒绝 |
| ROAD-03 / P0 | 业务模型、页面、命令适配 | 页面／阶段／错误 ID 脱离中文显示；旧中文值可解释且不改写历史原件 |
| ROAD-04 / P0 | i18n 模块与 Flutter | 中英文、类型化参数、语言包与缓存；热切换不丢 IME、草稿、滚动和任务上下文 |
| ROAD-05 / P0 | plugin_runtime、共享对象 | A/B 固定输入到实际任务；撤权、旧代次、异常读者、未释放映射等真实故障通过 |
| ROAD-06 / P0 | core/store、audit | 已发布材料退休与状态区分；容量满仍可合法收尾，无静默删除或伪完整证据 |
| ROAD-07 / P0 | network_node、SDK、平台文件层 | 正式资源／网络授权与持久意图；操作已发生但响应丢失能核对，不自动重复 |
| ROAD-08 / P0 | 证据捕获、独立工具 | A/B 依赖及内容投影完整包；移除原安装目录后仍可验证，重放不写生产库 |
| ROAD-09 / P1 | 通用 SDK、管理 UI | 必需依赖、公共附件／大正文／草稿能力；新插件无需工作台私有入口 |
| ROAD-10 / P1 | Flutter 工作台 | 虚拟化瀑布流、视图配置和预览预算；大规模及后台处理下保持锚点与输入可用 |
| ROAD-11 / P1 | 原生 ABI 与 runner | 同一业务样例原生变体；分域、限额、停机与原始产物兼容资格 |
| ROAD-12 / P0 持续 | core-web、移动与桌面后端 | 各平台“导入→创建→审计→终止→恢复”探针；缺失平台不写 PASS |

## 首先执行的组合

先完成 ROAD-01 与 ROAD-02/03 的接口边界；随后 ROAD-04 与 ROAD-05/06 可以在不争用同一契约的条件下并行。ROAD-07 从首个写请求开始包含恢复与证据，不等网络功能全部写完再补。

ROAD-08 是关键链路验收，不等待所有 UI 完美；ROAD-09/10 把既有能力转成真实使用价值。ROAD-12 从开始持续进行。ROAD-11 不应为了“支持三种后端”而抢占尚未闭合的内容与审计任务。

若新增任务需要改变已冻结契约、扩大权限、增加可信代码或修改历史格式，先补决策记录和负面夹具，再实现。不得只写 TODO 后继续扩大对外支持声明。

---

# 15. 验证体系、性能与发布门禁

## 15.1 必须分开的证据

源码编译、当前示例运行、旧二进制兼容、真实应用集成、真实进程故障、设备／系统恢复和渠道分发是不同证据。模拟后端不代替真实链路；构建成功不代替平台产品可用。

每项验证保存 commit、工具链、目标平台、feature 配置、产物摘要、测试入口、结果数量、退出状态和未覆盖范围。自有持久验证记录使用二进制格式，可派生 Markdown／HTML 报告；第三方工具原日志作为外部证据保留，不改写失败记录。

## 15.2 必测矩阵

| 范围 | 必须出现的负面场景 |
| --- | --- |
| 协议 | 截断、尾随字节、未知版本、非对齐、长度溢出、错误关联与 UInt64 边界 |
| 权限 | 自报身份、撤权后提交、范围外搜索／预览、依赖越权和外部效果升级 |
| 运行 | 准备期执行、无限循环、输出洪泛、取消、阻塞回调和迟到结果 |
| 共享对象 | 写后改动、假租约、旧代次、读者未释放、跨范围页面复用 |
| 数据与审计 | 提交前后崩溃、封存中断、磁盘满、签名错配、旧前缀回滚与缺证据 |
| i18n | 缺词、错误参数、资源越界、RTL、长文本、热切换、未知字段与旧 UI1 |
| 产品与平台 | 插件缺席、升级失败、后台终止、配额耗尽、真实旧库恢复和权限丢失 |

## 15.3 性能先建立基线

按低、中、高三类代表设备测冷启动、查询首批、连续滚动、输入响应、插件启动、数据复制、压缩／摘要、持久提交、证据增长和退出回收。用 100 / 1,000 / 10,000 卡及小／大附件组合，另测长期历史增长。

新门禁建议先采用相对回归预算：同设备同场景比较上一合格版本，并要求结果变化有解释。绝对延迟、内存和帧率目标在首次真实测量后写入发布配置，不把讨论中的示例值当成实测 SLA。

功能完成还应包含观察与维护入口：用户能区分正在执行、已提交、已封存、Unknown、容量满和材料已退休。测试失败不得通过减弱断言、重生成旧夹具或跳过真实后端来消除。

## 15.4 发布条件

M0–M7 的当前声明范围达标；无已知阻断级数据丢失、越权或历史伪报问题；恢复和兼容证据可复验；所有未完成能力显式实验化；首次全新安装、已有用户升级和插件缺席都可用。

Cloud 与公网服务可以独立发布，不扩大主应用的安全和平台声明。任何 new capability 需独立批准，不随自动更新静默启用。

---

# 16. 开发指导、协作规则与变更模板

## 16.1 编码原则

边界优先：每个模块只暴露必要类型，禁止 SDK 链接可信 Store；禁止 UI 直接读取私有数据库；禁止插件以地址、路径或来源字符串自行授予权限。

安全 Rust 区域保持默认无 unsafe；必要 FFI、mmap 和平台句柄集中在小模块，明确所有权、线程、失效、错误和销毁条件。安全审查不能只数 unsafe 行数，还要检查调用方能否破坏这些前置条件。

锁和事务只保护短操作；不等待插件、IO、语言包加载或用户交互。取消与执行完成分开，失败不统一转成“未提交”。控制消息与日志限额不能被插件子任务层层重置。

配置和 i18n 使用类型化字段；不以翻译结果判断状态，不解析错误文案决定重试。原件永久身份、内容修订和 UI 代次分别管理。

## 16.2 每次变更的交付卡

每个 PR 或本地合并至少写清：目标与关联工作包；允许修改目录；涉及的 schema／ABI／数据版本；威胁模型变化；迁移及失败语义；测试命令与实际结果；未运行项目；产物与来源摘要；剩余限制。

涉及外部效果时还要回答：用户到底批准了什么？最后授权在哪里检查？超时后如何知道是否执行？旧实例结果怎样失效？审计失败时还允许做什么？

涉及资源时回答：由谁创建、谁可以写、何时固定、引用如何计费、退出失败如何保留、什么证据允许回收？

## 16.3 AI／多人并行任务约束

建议每个代理只领取一个已确定接口下的实现或验证工作，不能自行扩展范围、改变协议、重写历史证据或宣布整套系统完成。核心 Schema、数据库迁移和权限决策保留单一整合责任人。

任务交接固定为：基线 SHA → 目标 → 明确非目标 → 可修改路径 → 接口假设 → 验收命令 → 禁止行为 → 结果证据。审查者至少独立复核一个关键负面场景，不仅阅读实现者的通过总结。

不假定并行工具会自动隔离输出目录。构建产物使用独立目录，最终发布按顺序生成并重算摘要，避免测试重链接覆盖待发布文件。

## 16.4 文档治理

ARCHITECTURE_BASELINE 记录硬约束；FUTURE_ROADMAP 记录任务与退出门槛；构建清单记录当前事实；reports 仅记录历史证据。版本变化更新当前入口，但不伪改历史报告的日期或状态。

源配置可读格式与平台构建格式是构建输入，不与二进制正式记录双写。不要因“避免 JSON”而重写所有成熟工具，也不要拿工具例外为自有运行期 JSON 旁路开口。

---

# 17. 风险、不妥之处与未来规划

## 17.1 当前主要风险

| 风险 | 不应采用的做法 | 收敛措施 |
| --- | --- | --- |
| 首版范围失控 | 同时重写核心、渲染器、协议编译器和全部平台后端 | 复用现有成果；以关键链路和工作包限制范围 |
| 原生共驻被误称隔离 | 用线程、Context、官方签名代替安全边界 | 按执行域与实际平台沙箱声明保证 |
| 固定模板锁死业务 | 每个插件新类型都要求核心认识全部语义 | 固定宿主协议，版本化业务契约 |
| 日志增长破坏可用性 | 永久录制全部 UI 噪声，满盘后无记录清理 | 业务边界审计、预算、证据复用与可审计退休 |
| 可重放承诺过度 | 将 Wasm、相同线程数或同版本号当成确定性证明 | 固定实际产物与上下文，分级报告 |
| i18n 破坏历史与兼容 | 全局替换中文、重签旧记录、覆盖冻结 schema | 新旧路径并存，语言只作用于表示或明确输出 |
| 平台／渠道后置 | Windows 完成后才试 iOS 或浏览器 | 从 M1 起持续探针与渠道资格验证 |
| 架构领先于产品 | 继续加底层模块而没有可用第三方流程 | 以用户卡片、真实插件和恢复场景验收 |

## 17.2 未来功能层次

**近期：可靠可用。** 完成中英文底座、公共 IO 与结果核对、证据清理、第三方依赖管理、草稿和虚拟化瀑布流；让现有 Windows 主路径更完整，并保持非 Windows 探针。

**中期：可移植平台。** 根据实际平台资格推广新核心，稳定分层 SDK、提供可独立使用的开发工具包；按需要交付受控 native-runner，验证 Cloud 普通插件及其他真实服务提供者。

**长期：生态与高阶能力。** 插件发现与分发、专业类型、跨设备工作流、可选节点协作、高性能计算后端与更强独立见证。每项按能力 profile 发布，不让完整生态成为本地基本功能的启动前提。

不预设下一稳定版本号、最终 test 序号或工期；版本与发布日期依据可复验证据，而不是以模块数量或讨论轮数决定。

## 17.3 本轮总体选择

保留 Flutter；延续可移植 Rust 核心；Wasm 默认、原生受控补充；语言资源是核心管理的数据包；平台能力显式分级；Cloud 独立；协议与审计规则不因平台、语言或执行后端而降低。

**最终验收不是“写了多少组件”，而是同一份内容能否在插件变化、语言切换、进程故障、数据迁移与平台差异中保持可解释、可恢复、可验证。**

---

# 来源与使用边界

所有仓库条目固定在 `0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7`。官方资料核对日期为 2026-09-15；移动渠道政策在实际提交前再次复核。本文没有执行仓库测试，也不将工具或文档描述转化为已通过的产品承诺。

## 仓库依据

[R1] GitHub main 分支元数据：本次核对提交及来源。
https://api.github.com/repos/StarrySky7D4/morrow/branches/main

[R2] pubspec.yaml：应用版本与依赖清单。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/pubspec.yaml

[R3] docs/FUTURE_ROADMAP.md，第 1–112 行：当前起点、M0–M7 与发布规则。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/docs/FUTURE_ROADMAP.md

[R4] sdk/README.md，第 1–77 行：SDK、Wasm、原生 ABI 与 IO 的实际范围。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/sdk/README.md

[R5] docs/PLUGIN_SDK_COMPATIBILITY.md，第 1–32 行：冻结原件与精确摘要规则。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/docs/PLUGIN_SDK_COMPATIBILITY.md

[R6] lib/pages/workspace_pages.dart，第 1–69 行：中文业务状态与命令耦合。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/lib/pages/workspace_pages.dart

[R7] sdk/rust/contracts/ui.capnp：UI1 字面文本与事件字段。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/sdk/rust/contracts/ui.capnp

[R8] network_node/README.md，第 1–29 行：test.51 原生节点和验证边界。
https://github.com/StarrySky7D4/morrow/blob/0e93e49f56dd2534db5a0cd8fb31e9d5c705dfa7/network_node/README.md

---

# 官方技术依据

[W1] Flutter — Internationalizing Flutter apps。用于 Localizations、delegate、locale 与自有资源适配。
https://docs.flutter.dev/ui/internationalization

[W2] Cap’n Proto — Schema Language。用于追加式协议演进与版本规则。
https://capnproto.org/language.html

[W3] Protocol Buffers — Proto Serialization Is Not Canonical。用于区分原始字节证据与语义身份。
https://protobuf.dev/programming-guides/serialization-not-canonical/

[W4] Wasmtime — Deterministic Wasm Execution。用于确定性执行的条件与局限；不表示选定 Wasmtime 为唯一运行时。
https://docs.wasmtime.dev/examples-deterministic-wasm-execution.html

[W5] Microsoft Learn — Processes and Threads。用于区分进程执行环境、线程与线程池的职责。
https://learn.microsoft.com/en-us/windows/win32/procthread/processes-and-threads

[W6] Unicode UTS #35 — LDML。用于语言标识、匹配与区域数据规则。
https://unicode.org/reports/tr35/

[W7] Flutter — Performance best practices。用于惰性列表、布局和重建的验证方向。
https://docs.flutter.dev/perf/best-practices

[W8] MDN — SharedArrayBuffer。用于浏览器共享可写内存与部署条件，不将其等同原生只读 mmap。
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/SharedArrayBuffer

[W9] Apple — App Review Guidelines。用于插件和动态代码分发资格的独立复核；不保证具体方案通过审核。
https://developer.apple.com/app-store/review/guidelines/

[W10] Google Play — Device and Network Abuse。用于动态代码分发与设备／网络行为的渠道约束复核。
https://support.google.com/googleplay/android-developer/answer/9888379?hl=en

## 文档维护约定

讨论中的硬约束来自用户；方案选择来自本文件建议；现状事实来自固定提交。后续实现记录若与本文不同，应更新当前状态与相应决策，不回写旧证据，不凭文档措辞宣布完成。

**结束语：先稳住身份、权限、提交和证据，再扩大能力与平台；i18n 从协议和数据语义开始，不成为最后一层翻译补丁。**
