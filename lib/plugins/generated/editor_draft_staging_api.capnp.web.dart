// Generated by tool/generate_workbench_client.py; do not edit.
// Optional upstream int-based reflection is unavailable on Web.
// Binary readers, builders, field offsets and schema validation are unchanged.
// Use BigInt fields for exact 64-bit values; legacy int access rejects unsafe JS values.
// Generated by capnpc-dart — do not edit by hand.
// ignore_for_file: annotate_overrides, unnecessary_this

import 'dart:typed_data';
import 'package:capnproto_dart/capnproto_dart.dart';

enum Phase { pending, ready, retired }

Phase? phaseFromUint16(int v) =>
    v < Phase.values.length ? Phase.values[v] : null;
int phaseToUint16(Phase v) => v.index;

enum ResultKind {
  absent,
  record,
  list,
  exported,
  reconciled,
  decision,
  decisions,
  scopes,
}

ResultKind? resultKindFromUint16(int v) =>
    v < ResultKind.values.length ? ResultKind.values[v] : null;
int resultKindToUint16(ResultKind v) => v.index;

enum DecisionStatus { pending, committed, cancelled, conflict }

DecisionStatus? decisionStatusFromUint16(int v) =>
    v < DecisionStatus.values.length ? DecisionStatus.values[v] : null;
int decisionStatusToUint16(DecisionStatus v) => v.index;

final class ImportRequestReader extends StructReader {
  ImportRequestReader(super.raw, {super.capabilities});

  int get version => getUint16Field(0);

  Uint8List? get digest => getDataField(0);

  String? get cardId => getTextField(1);

  String? get draftId => getTextField(2);

  String? get operation => getTextField(3);

  int get expectedGeneration => _checkedWireInt(expectedGenerationBigInt);
  BigInt get expectedGenerationBigInt =>
      ((BigInt.from(getUint32Field(12).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(8).toUnsigned(32)));

  String? get name => getTextField(4);

  String? get kind => getTextField(5);

  int get bytes => _checkedWireInt(bytesBigInt);
  BigInt get bytesBigInt =>
      ((BigInt.from(getUint32Field(20).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(16).toUnsigned(32)));

  Uint8List? get sha256 => getDataField(6);
}

final class ImportRequestBuilder extends StructBuilder {
  ImportRequestBuilder(super.raw);

  @override
  ImportRequestReader asReader() => ImportRequestReader(rawToReader());

  set version(int v) {
    setUint16Field(0, v);
  }

  set digest(Uint8List? v) {
    setDataField(0, v);
  }

  set cardId(String? v) {
    setTextField(1, v);
  }

  set draftId(String? v) {
    setTextField(2, v);
  }

  set operation(String? v) {
    setTextField(3, v);
  }

  set expectedGeneration(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    expectedGenerationBigInt = BigInt.from(v).toUnsigned(64);
  }

  set expectedGenerationBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field expectedGeneration');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(8, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(12, (bits >> 32).toInt());
  }

  set name(String? v) {
    setTextField(4, v);
  }

  set kind(String? v) {
    setTextField(5, v);
  }

  set bytes(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    bytesBigInt = BigInt.from(v).toUnsigned(64);
  }

  set bytesBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field bytes');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(16, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(20, (bits >> 32).toInt());
  }

  set sha256(Uint8List? v) {
    setDataField(6, v);
  }
}

final class _ImportRequestFactory
    extends StructFactory<ImportRequestReader, ImportRequestBuilder> {
  @override
  int get dataWords => 3;
  @override
  int get ptrWords => 7;
  @override
  ImportRequestReader fromRawReader(RawStructReader r) =>
      ImportRequestReader(r);
  @override
  ImportRequestReader fromRawReaderWithCapabilities(
    RawStructReader r,
    List<Object?> capabilities,
  ) => ImportRequestReader(r, capabilities: capabilities);
  @override
  ImportRequestBuilder fromRawBuilder(RawStructBuilder r) =>
      ImportRequestBuilder(r);
}

final importRequestFactory = _ImportRequestFactory();

final class RecordReader extends StructReader {
  RecordReader(super.raw, {super.capabilities});

  ImportRequestReader? get request => getStructFieldWith(
    0,
    (r) => ImportRequestReader(r, capabilities: capabilityTable),
  );

  String? get assetId => getTextField(1);

  Phase? get phase => phaseFromUint16(getUint16Field(0));

  bool get currentActive => getBoolField(16);

  bool get bytesRetained => getBoolField(17);

  int get stagingRevision => _checkedWireInt(stagingRevisionBigInt);
  BigInt get stagingRevisionBigInt =>
      ((BigInt.from(getUint32Field(12).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(8).toUnsigned(32)));

  bool get repeated => getBoolField(18);

  int get currentGeneration => _checkedWireInt(currentGenerationBigInt);
  BigInt get currentGenerationBigInt =>
      ((BigInt.from(getUint32Field(20).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(16).toUnsigned(32)));

  bool get mainActive => getBoolField(19);
}

final class RecordBuilder extends StructBuilder {
  RecordBuilder(super.raw);

  @override
  RecordReader asReader() => RecordReader(rawToReader());

  ImportRequestBuilder initRequest() {
    return initStructFieldWith(0, (r) => ImportRequestBuilder(r), 3, 7);
  }

  bool hasRequest() => hasPointerField(0);

  set assetId(String? v) {
    setTextField(1, v);
  }

  set phase(Phase v) {
    setUint16Field(0, phaseToUint16(v));
  }

  set currentActive(bool v) {
    setBoolField(16, v);
  }

  set bytesRetained(bool v) {
    setBoolField(17, v);
  }

  set stagingRevision(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    stagingRevisionBigInt = BigInt.from(v).toUnsigned(64);
  }

  set stagingRevisionBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field stagingRevision');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(8, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(12, (bits >> 32).toInt());
  }

  set repeated(bool v) {
    setBoolField(18, v);
  }

  set currentGeneration(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    currentGenerationBigInt = BigInt.from(v).toUnsigned(64);
  }

  set currentGenerationBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field currentGeneration');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(16, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(20, (bits >> 32).toInt());
  }

  set mainActive(bool v) {
    setBoolField(19, v);
  }
}

final class _RecordFactory extends StructFactory<RecordReader, RecordBuilder> {
  @override
  int get dataWords => 3;
  @override
  int get ptrWords => 2;
  @override
  RecordReader fromRawReader(RawStructReader r) => RecordReader(r);
  @override
  RecordReader fromRawReaderWithCapabilities(
    RawStructReader r,
    List<Object?> capabilities,
  ) => RecordReader(r, capabilities: capabilities);
  @override
  RecordBuilder fromRawBuilder(RawStructBuilder r) => RecordBuilder(r);
}

final recordFactory = _RecordFactory();

final class EnvelopeReader extends StructReader {
  EnvelopeReader(super.raw, {super.capabilities});

  int get version => getUint16Field(0);

  Uint8List? get digest => getDataField(0);

  ResultKind? get kind => resultKindFromUint16(getUint16Field(2));

  String? get cardId => getTextField(1);

  String? get draftId => getTextField(2);

  String? get operation => getTextField(3);

  int get expectedGeneration => _checkedWireInt(expectedGenerationBigInt);
  BigInt get expectedGenerationBigInt =>
      ((BigInt.from(getUint32Field(12).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(8).toUnsigned(32)));

  String? get importOperation => getTextField(4);

  int get currentGeneration => _checkedWireInt(currentGenerationBigInt);
  BigInt get currentGenerationBigInt =>
      ((BigInt.from(getUint32Field(20).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(16).toUnsigned(32)));

  bool get mainActive => getBoolField(32);

  int get stagingRevision => _checkedWireInt(stagingRevisionBigInt);
  BigInt get stagingRevisionBigInt =>
      ((BigInt.from(getUint32Field(28).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(24).toUnsigned(32)));

  RecordReader? get record => getStructFieldWith(
    5,
    (r) => RecordReader(r, capabilities: capabilityTable),
  );

  ListReader<RecordReader>? get records => getStructListFieldWith(
    6,
    (r) => RecordReader(r, capabilities: capabilityTable),
  );

  Uint8List? get exportSha256 => getDataField(7);

  int get exportBytes => _checkedWireInt(exportBytesBigInt);
  BigInt get exportBytesBigInt =>
      ((BigInt.from(getUint32Field(36).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(32).toUnsigned(32)));

  DecisionReader? get decision => getStructFieldWith(
    8,
    (r) => DecisionReader(r, capabilities: capabilityTable),
  );

  ListReader<DecisionReader>? get decisions => getStructListFieldWith(
    9,
    (r) => DecisionReader(r, capabilities: capabilityTable),
  );

  String? get nextCursor => getTextField(10);

  String? get requestCursor => getTextField(11);

  int get requestLimit => getUint32Field(40);

  ListReader<ScopeReader>? get scopes => getStructListFieldWith(
    12,
    (r) => ScopeReader(r, capabilities: capabilityTable),
  );
}

final class EnvelopeBuilder extends StructBuilder {
  EnvelopeBuilder(super.raw);

  @override
  EnvelopeReader asReader() => EnvelopeReader(rawToReader());

  set version(int v) {
    setUint16Field(0, v);
  }

  set digest(Uint8List? v) {
    setDataField(0, v);
  }

  set kind(ResultKind v) {
    setUint16Field(2, resultKindToUint16(v));
  }

  set cardId(String? v) {
    setTextField(1, v);
  }

  set draftId(String? v) {
    setTextField(2, v);
  }

  set operation(String? v) {
    setTextField(3, v);
  }

  set expectedGeneration(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    expectedGenerationBigInt = BigInt.from(v).toUnsigned(64);
  }

  set expectedGenerationBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field expectedGeneration');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(8, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(12, (bits >> 32).toInt());
  }

  set importOperation(String? v) {
    setTextField(4, v);
  }

  set currentGeneration(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    currentGenerationBigInt = BigInt.from(v).toUnsigned(64);
  }

  set currentGenerationBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field currentGeneration');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(16, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(20, (bits >> 32).toInt());
  }

  set mainActive(bool v) {
    setBoolField(32, v);
  }

  set stagingRevision(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    stagingRevisionBigInt = BigInt.from(v).toUnsigned(64);
  }

  set stagingRevisionBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field stagingRevision');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(24, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(28, (bits >> 32).toInt());
  }

  RecordBuilder initRecord() {
    return initStructFieldWith(5, (r) => RecordBuilder(r), 3, 2);
  }

  bool hasRecord() => hasPointerField(5);

  ListBuilder<RecordBuilder> initRecords(int length) {
    return initStructListFieldWith(6, length, (r) => RecordBuilder(r), 3, 2);
  }

  set exportSha256(Uint8List? v) {
    setDataField(7, v);
  }

  set exportBytes(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    exportBytesBigInt = BigInt.from(v).toUnsigned(64);
  }

  set exportBytesBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field exportBytes');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(32, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(36, (bits >> 32).toInt());
  }

  DecisionBuilder initDecision() {
    return initStructFieldWith(8, (r) => DecisionBuilder(r), 6, 2);
  }

  bool hasDecision() => hasPointerField(8);

  ListBuilder<DecisionBuilder> initDecisions(int length) {
    return initStructListFieldWith(9, length, (r) => DecisionBuilder(r), 6, 2);
  }

  set nextCursor(String? v) {
    setTextField(10, v);
  }

  set requestCursor(String? v) {
    setTextField(11, v);
  }

  set requestLimit(int v) {
    setUint32Field(40, v);
  }

  ListBuilder<ScopeBuilder> initScopes(int length) {
    return initStructListFieldWith(12, length, (r) => ScopeBuilder(r), 0, 2);
  }
}

final class _EnvelopeFactory
    extends StructFactory<EnvelopeReader, EnvelopeBuilder> {
  @override
  int get dataWords => 6;
  @override
  int get ptrWords => 13;
  @override
  EnvelopeReader fromRawReader(RawStructReader r) => EnvelopeReader(r);
  @override
  EnvelopeReader fromRawReaderWithCapabilities(
    RawStructReader r,
    List<Object?> capabilities,
  ) => EnvelopeReader(r, capabilities: capabilities);
  @override
  EnvelopeBuilder fromRawBuilder(RawStructBuilder r) => EnvelopeBuilder(r);
}

final envelopeFactory = _EnvelopeFactory();

final class DecisionReader extends StructReader {
  DecisionReader(super.raw, {super.capabilities});

  ImportRequestReader? get request => getStructFieldWith(
    0,
    (r) => ImportRequestReader(r, capabilities: capabilityTable),
  );

  String? get operation => getTextField(1);

  int get expectedGeneration => _checkedWireInt(expectedGenerationBigInt);
  BigInt get expectedGenerationBigInt =>
      ((BigInt.from(getUint32Field(4).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(0).toUnsigned(32)));

  DecisionStatus? get status => decisionStatusFromUint16(getUint16Field(8));

  int get currentGeneration => _checkedWireInt(currentGenerationBigInt);
  BigInt get currentGenerationBigInt =>
      ((BigInt.from(getUint32Field(20).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(16).toUnsigned(32)));

  bool get mainActive => getBoolField(80);

  int get stagingRevision => _checkedWireInt(stagingRevisionBigInt);
  BigInt get stagingRevisionBigInt =>
      ((BigInt.from(getUint32Field(28).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(24).toUnsigned(32)));

  int get decisionRevision => _checkedWireInt(decisionRevisionBigInt);
  BigInt get decisionRevisionBigInt =>
      ((BigInt.from(getUint32Field(36).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(32).toUnsigned(32)));

  int get committedRevision => _checkedWireInt(committedRevisionBigInt);
  BigInt get committedRevisionBigInt =>
      ((BigInt.from(getUint32Field(44).toUnsigned(32)) << 32) |
      BigInt.from(getUint32Field(40).toUnsigned(32)));
}

final class DecisionBuilder extends StructBuilder {
  DecisionBuilder(super.raw);

  @override
  DecisionReader asReader() => DecisionReader(rawToReader());

  ImportRequestBuilder initRequest() {
    return initStructFieldWith(0, (r) => ImportRequestBuilder(r), 3, 7);
  }

  bool hasRequest() => hasPointerField(0);

  set operation(String? v) {
    setTextField(1, v);
  }

  set expectedGeneration(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    expectedGenerationBigInt = BigInt.from(v).toUnsigned(64);
  }

  set expectedGenerationBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field expectedGeneration');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(0, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(4, (bits >> 32).toInt());
  }

  set status(DecisionStatus v) {
    setUint16Field(8, decisionStatusToUint16(v));
  }

  set currentGeneration(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    currentGenerationBigInt = BigInt.from(v).toUnsigned(64);
  }

  set currentGenerationBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field currentGeneration');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(16, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(20, (bits >> 32).toInt());
  }

  set mainActive(bool v) {
    setBoolField(80, v);
  }

  set stagingRevision(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    stagingRevisionBigInt = BigInt.from(v).toUnsigned(64);
  }

  set stagingRevisionBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field stagingRevision');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(24, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(28, (bits >> 32).toInt());
  }

  set decisionRevision(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    decisionRevisionBigInt = BigInt.from(v).toUnsigned(64);
  }

  set decisionRevisionBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field decisionRevision');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(32, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(36, (bits >> 32).toInt());
  }

  set committedRevision(int v) {
    if (BigInt.from(v).abs() > BigInt.from(9007199254740991)) {
      throw RangeError('Use the BigInt field for an exact 64-bit value');
    }
    committedRevisionBigInt = BigInt.from(v).toUnsigned(64);
  }

  set committedRevisionBigInt(BigInt v) {
    if (v < BigInt.zero || v > (BigInt.one << 64) - BigInt.one) {
      throw RangeError('64-bit field committedRevision');
    }
    final bits = v.toUnsigned(64);
    setUint32Field(40, (bits & BigInt.from(0xffffffff)).toInt());
    setUint32Field(44, (bits >> 32).toInt());
  }
}

final class _DecisionFactory
    extends StructFactory<DecisionReader, DecisionBuilder> {
  @override
  int get dataWords => 6;
  @override
  int get ptrWords => 2;
  @override
  DecisionReader fromRawReader(RawStructReader r) => DecisionReader(r);
  @override
  DecisionReader fromRawReaderWithCapabilities(
    RawStructReader r,
    List<Object?> capabilities,
  ) => DecisionReader(r, capabilities: capabilities);
  @override
  DecisionBuilder fromRawBuilder(RawStructBuilder r) => DecisionBuilder(r);
}

final decisionFactory = _DecisionFactory();

final class ScopeReader extends StructReader {
  ScopeReader(super.raw, {super.capabilities});

  String? get cardId => getTextField(0);

  String? get draftId => getTextField(1);
}

final class ScopeBuilder extends StructBuilder {
  ScopeBuilder(super.raw);

  @override
  ScopeReader asReader() => ScopeReader(rawToReader());

  set cardId(String? v) {
    setTextField(0, v);
  }

  set draftId(String? v) {
    setTextField(1, v);
  }
}

final class _ScopeFactory extends StructFactory<ScopeReader, ScopeBuilder> {
  @override
  int get dataWords => 0;
  @override
  int get ptrWords => 2;
  @override
  ScopeReader fromRawReader(RawStructReader r) => ScopeReader(r);
  @override
  ScopeReader fromRawReaderWithCapabilities(
    RawStructReader r,
    List<Object?> capabilities,
  ) => ScopeReader(r, capabilities: capabilities);
  @override
  ScopeBuilder fromRawBuilder(RawStructBuilder r) => ScopeBuilder(r);
}

final scopeFactory = _ScopeFactory();

int _checkedWireInt(BigInt value) {
  final signed = value.toSigned(64);
  if (signed.abs() > BigInt.from(9007199254740991)) {
    throw RangeError('Use the BigInt field for an exact 64-bit value');
  }
  return signed.toInt();
}

const schemaReflectionAvailable = false;
