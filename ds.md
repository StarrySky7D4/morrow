**不需要因为 Android“默认单进程”，就把所有 runner 并入 Flutter 主进程。Android 支持一个应用运行多个进程，也支持具有独立权限边界的隔离服务。**“默认共用一个进程”和“只能有一个进程”是两回事。

对于 Morrow，我建议：

> **runner 的代码随同一个 APK 打包，运行时由 Android 的隔离 Service 承载；插件管理、资料库、权限代理和审计仍由可信宿主管理。**

也就是：**合并部署，不默认合并执行进程。** 同时，不必照搬 Windows 的“独立 EXE＋启动子进程”实现方式。

以下是建议的 Android 落地方案，不是当前仓库已经完成的状态。

---

## 一、先区分三个“合并”

| 合并对象 | 我的建议 | 原因 |
|---|---|---|
| **runner 源码与公共执行库** | 可以统一 | 桌面与 Android 复用任务、协议、Wasm 执行和限额逻辑 |
| **runner 与主应用的安装包** | 应当合并 | 随 APK 提供可信运行器和原生库，不需要另装一个应用 |
| **外部插件与 Flutter 主进程** | 不默认合并 | 避免插件原生异常、运行时故障和资源失控直接影响内容核心 |

还可以进一步拆开：

**`RunnerManager`** 管理实例、授权、任务、依赖和资源预算，适合留在可信宿主。

**`RunnerEngine`** 执行插件代码，放进隔离服务进程。

因此，Android 上可以把 runner 编译成 `libmorrow_runner.so`，由服务中的 Kotlin／JNI 适配加载。**`.so` 被装进同一个 APK，不意味着必须被 Flutter 所在进程加载。**

这是应用组织方式的选择：Android 的组件可以通过 Manifest 指定所属进程，系统按需创建对应进程。

---

## 二、建议的进程布局：一个可信宿主，少量按需执行域

推荐首期采用：

```text
Morrow APK
│
├─ 主进程：可信宿主
│   ├─ Flutter UI
│   ├─ Rust 内容核心
│   ├─ 插件管理、权限与依赖管理
│   ├─ 数据库、审计与证据
│   └─ Android 文件、网络、凭据等能力代理
│
├─ 隔离服务进程：执行域 A，按需启动
│   ├─ 薄 Kotlin Service／Binder 层
│   ├─ Rust RunnerEngine
│   ├─ Wasm 插件实例
│   └─ 有界任务与计算线程
│
└─ 隔离服务进程：执行域 B，确有需求时启动
    ├─ 同一份 RunnerEngine
    └─ 需要与 A 分离的插件及依赖
```

### 1. 不给每个 runner 启动一套 Flutter

runner 进程只需要服务适配、协议处理和执行引擎，不需要创建 FlutterEngine、窗口、播放器界面或完整工作台。

也应将应用初始化分成两条路径：**主进程初始化内容核心；runner 进程只初始化运行器。** 避免每个进程都自动打开资料库、注册网络任务、读取凭据或初始化整套依赖。

这比直接把桌面宿主程序整体移入 Android 服务更适合控制复杂度和资源开销。

### 2. 不把数据库和签名能力交给 runner

建议隔离进程不持有正式数据库连接、审计私钥、账号令牌或任意目录权限。

它需要读取卡片、处理附件或访问网络时，向可信宿主提出请求，由宿主检查并执行。**MorrowCloud 也走这条路径：云业务在插件中，网络与凭据使用由宿主代理。**

Android 的 `isolatedProcess` 服务运行在特殊隔离进程中，没有自身的应用权限，适合以服务接口承接受控操作；但宿主交给它的 Binder 能力、文件描述符和数据仍然需要严格限制。

---

## 三、普通远程 Service 与隔离 Service，不能混为一谈

Android 上至少有下面三种安排：

| 方式 | 故障与权限边界 | 适合用途 |
|---|---|---|
| **普通 Service，不指定进程** | 默认仍在应用主进程中 | 可信的平台协调，不用于隔离不可信插件 |
| **`android:process=":runner"`** | 独立地址空间，但通常仍属于应用自身的 UID／权限范围 | 可信媒体或计算组件的故障分离 |
| **再加 `android:isolatedProcess="true"`** | 独立进程和特殊隔离身份，不继承普通应用权限 | 外部插件执行器的优先候选 |

`android:process` 决定组件运行位置；`isolatedProcess` 决定服务是否采用特殊隔离环境，二者不是同一个开关。

**只写 `:runner`，不足以证明原生插件无法读取 Morrow 的私有文件或使用应用权限。** 对需要防止权限旁路的外部插件，不能把普通远程服务当成完整沙箱。

### 推荐的组件声明

以下只展示服务声明，尚不包含通信与生命周期实现：

```xml
<service
    android:name=".runtime.PluginRunnerService"
    android:process=":plugin_runner"
    android:isolatedProcess="true"
    android:exported="false" />
```

宿主使用明确的组件名绑定，不开放为任意其他应用可调用的服务；插件包中的自报身份不作为授权依据。Android 官方也要求安全地使用显式 Intent 绑定服务。

### 一个服务声明能否运行多个隔离实例？

**Android 10／API 29 起，可以使用 `bindIsolatedService()`，从一个隔离服务组件声明创建多个并发实例。** 不同 `instanceName` 对应不同实例，因此不必为每个插件写一个新的 Service 类。

我建议实例名对应**宿主创建的执行域代次**，而不是直接使用插件 ID：

```text
domain_7_epoch_1
domain_9_epoch_1
```

域重建时使用新代次，重新建立绑定与授权。

对更旧版本，可以预先声明有限数量的隔离服务槽位，复用同一实现代码。**槽位不足时排队或明确拒绝，不把原本要求隔离的插件悄悄放回主进程。**

---

## 四、Binder 不替代 Cap’n Proto，负责承载它

Android 不必重新设计一套业务协议。

建议分工如下：

| 内容 | 承载方式 |
|---|---|
| 建立连接、回调、状态通知、取消 | Binder／薄 AIDL 接口 |
| 小型结构化请求与响应 | Binder 中传递 Cap’n Proto 二进制帧 |
| 大对象、附件片段、批量计算输入 | 受控 `SharedMemory`／文件描述符＋Cap’n Proto 描述 |
| 本地内容、配置、审计、证据 | 仍由核心按 Protobuf＋LZ4 保存 |

**AIDL 只描述少量平台承载接口，不再定义第二套卡片、权限和任务业务模型，也不引入 JSON。**

### 1. 不通过 Binder 一次发送大附件

Binder 的事务缓冲区大小有限，当前约为 1 MB，而且由进程中进行中的事务共享，不是每条请求都独享 1 MB。因此，即使单条消息不大，并发过多仍可能失败。

建议小消息保留现有有界帧，大对象通过共享对象传递。不要把一张大图片转换成 `byte[]` 后塞进一次 Binder 调用。

而且，Binder 传输失败可能发生在请求阶段，也可能发生在响应阶段。**不能把传输异常统一当成“核心没有提交”。** 仍需使用稳定操作 ID 查询结果。

### 2. Android 共享内存能够承接原有设计

Java `SharedMemory` 从 API 27 提供；NDK 的 `ASharedMemory` 从 API 26 提供，并允许通过 `ParcelFileDescriptor` 等机制交付描述符。

建议输入发布流程为：

**宿主分配 → 写入 → 释放全部可写映射 → 降为只读并检查结果 → 校验固定内容 → 交付给指定 runner。**

需要特别注意，`setProtect()` 只影响之后建立的映射，已有映射保留原权限。**仅调用一次 `setProtect(PROT_READ)`，不能证明所有旧写入者已经消失。**

runner 返回结果时也必须固定后再进入内容提交。不能相信插件口头声明“我已经不写了”，就直接对仍可变的数据签名。

### 3. Binder 回调不是计算线程池

Android 的 Service 生命周期回调与远程 Binder 方法有不同的线程行为，远程方法还可能被并发调用。不能在 `onCreate()`、`onBind()` 或 Binder 回调中直接执行长时间插件计算。

建议这些入口只做：

**验证连接与消息 → 申请预算 → 入队 → 尽快返回。**

实际任务在 runner 的受控执行器中运行，结果异步回传。等待插件期间不占用内容事务，也不持有全局锁。

---

## 五、多插件与线程的规则，Android 上继续成立

不必变成“每个插件一个 Service、一个进程、一条线程”。

建议仍按此前的执行域设计：

**轻量依赖库**进入同一可信依赖集合，在当前任务线程上执行。

**有状态但不要求固定线程的插件**使用实例串行队列，复用执行器。

**计算插件**申请有界并行许可，避免每个插件按整机 CPU 数各建一个线程池。

**互不信任的原生组件**进入不同隔离进程，不用独立线程代替内存隔离。

对于多个 Wasm 插件，可以在一个 runner 内使用独立实例和受控导入，减少进程数量；但它们仍共享运行器进程的故障范围。Wasm 的内存与能力隔离依赖运行时和宿主导入的正确实现，不能据此宣称整个进程永远不会失败。

### 资源不足时怎么处理？

我建议按需启动，先从一个活跃执行域验证，再根据任务与设备预算增加并发域。

资源压力下优先：

**减少并行度、暂停预览任务、排队、释放空闲实例。**

**不能为了节省内存，把必须隔离的原生插件自动合并到同一个进程。** 这会改变安全语义，而不仅是降低性能。

另外，Android 支持多个隔离服务共驻进程的特定绑定选项，但它不是默认的“省内存按钮”。Morrow 若要合并执行域，必须先确认它们可以共同承担地址空间和故障风险。

---

## 六、并入主进程的 runner，可以作为有条件的模式

**技术上可以在 Flutter 主进程中加载 Rust runner 库，再在工作线程上运行 Wasm。** 但我不建议把它当作外部插件的默认部署。

可以考虑以下分级：

| 模式 | 我的建议 |
|---|---|
| 可信、随包提供的短小纯函数 | 可以直接在可信核心工作线程执行，不必包装成外部 runner |
| 受限 Wasm 进程内执行 | 可以作为明确声明的轻量配置，接受与宿主共享进程故障范围 |
| 第三方 Wasm 执行 | 默认优先隔离 Service，尤其是需要强制停止或处理不可信输入时 |
| 第三方原生动态库 | 不并入主进程；另行验证隔离执行与分发方式 |
| 任意下载的原生可执行程序 | 不作为 Android 普通插件的默认路线 |

**Wasm trap 可以由运行时处理，不等于原生运行时崩溃、宿主函数错误和进程级内存压力都能局部恢复。** 因此进程内配置必须有不同的故障隔离等级，不能与隔离服务配置使用相同承诺。

此外，runner 的部署方式与插件的分发许可是两回事。Google Play 对从渠道外下载 dex、JAR、`.so` 等可执行代码有限制；解释器／虚拟机代码也仍受相应政策约束。**采用隔离 Service，不会自动让任意原生插件下载变得可发布。**

对 Morrow，建议原生 runner 和需要的可信库随应用发布，普通动态业务插件继续优先使用受限 Wasm。

---

## 七、真正要解决的不是单进程，而是 Android 的生命周期

**增加进程并不能保证后台永久存活；把 runner 并入主进程也不能解决这个问题。** Android 根据组件状态和资源需要管理进程，系统结束进程时不保证执行 `onDestroy()`。

因此需要把两件事分开：

### 1. 执行隔离

由隔离 Service、Wasm 实例、能力代理和资源边界实现。

### 2. 后台调度与恢复

由适用的 WorkManager、前台服务或其他平台任务机制提供运行机会，并由 Morrow 的持久意图、检查点和操作结果恢复状态。

WorkManager 面向可持久调度的工作；前台服务也受到启动条件、服务类型等限制。它们不是无限常驻 runner 的替代品。

我建议短期按以下策略实现：

**前台交互任务**由宿主绑定 runner；没有待执行任务时释放。

**可延期任务**持久化意图，由系统调度触发后再绑定 runner。

**用户明确发起的持续任务**采用适用的平台机制，但仍持续保存检查点，允许被取消或中断。

系统重新提供执行机会后，**先核对原操作，再决定继续或重新执行**，不能把系统重新启动组件理解成用户重新发出了一次命令。

---

## 八、异常与退出，需要一个 Android 专用适配层

建议状态流程保持原来的业务语义：

```text
停止接受新任务
  → 撤销新操作权限
  → 请求取消／限时排空
  → 核对已提交及未决操作
  → 释放绑定和资源
  → 确认旧实例失效
  → 必要时创建新执行域
```

但以下细节不能直接照搬桌面：

### 服务重新连接，不等于原实例恢复

`onServiceDisconnected()` 通常表示进程崩溃或被终止；原绑定可能仍然存在，服务再次运行后可以重新回调连接。`onBindingDied()` 则需要解除并重新绑定。

建议每次重新连接都重新核对包、协议、实例代次和权限。旧任务结果只能进入结果核对流程，不能沿用旧授权形成新提交。

### 解除绑定，不等于已经证明所有资源回收

正常 `unbindService()` 不会触发 `onServiceDisconnected()`。绑定生命周期和进程／共享对象生命周期必须分别处理，不能只等一个回调就宣布页面可以复用。

共享对象无法确认释放时，继续保守计费，不跨权限范围复用。

### 不把 `killProcess(pid)` 当成对隔离进程的通用管理接口

该 API 仍受到内核权限限制；不能只因为服务来自同一个 APK，就假定宿主能够任意向隔离 UID 的进程发信号。

首版应通过服务管理、可信 runner 的退出机制和实例撤权完成停止，并对**无响应原生代码的最终终止与回收**进行专门实机验证。在通过之前，不能声称“任意原生插件都可以在指定毫秒内强制结束”。

即使物理终止尚待确认，宿主也必须能够先保证：

> **这个实例不能再取得新资源，不能再发起新业务操作，迟到结果不能以旧授权提交。**

---

## 九、建议的实施顺序

| 阶段 | 交付内容 | 重点验收 |
|---|---|---|
| **第一步** | 将 runner 逻辑提取为可复用 Rust 库，增加 Kotlin Service 外壳 | runner 不初始化 Flutter、不打开正式资料库 |
| **第二步** | 一个隔离服务实例＋Cap’n Proto 双向调用 | 真实隔离身份、越权拒绝、执行异常不直接终止 UI |
| **第三步** | SharedMemory 固定输入与结果交付 | 旧可写映射、越界、过期句柄和未释放资源 |
| **第四步** | 多执行域、任务队列与依赖调用 | 一域停止不误停其他域，避免嵌套并行和等待死锁 |
| **第五步** | 后台任务与重启恢复 | 在调用前、提交后、返回前终止进程，均能准确核对结果 |
| **第六步** | 低内存及进程内兼容配置 | 以实际开销决定优化，不降低已经声明的隔离等级 |

同时验证较旧 Android 的固定服务槽位和传输降级，不把某个 API 缺失转化成未经说明的主进程执行。

---

## 结论

**不建议直接把所有 runner 并入 Flutter 主进程。更适合 Morrow 的方案是：**

> **RunnerManager 与可信内容核心留在宿主；RunnerEngine 编译为原生库，随 APK 发布，在 Android 管理的隔离 Service 进程中按需运行；通过 Binder 承载 Cap’n Proto 控制消息，通过受控 SharedMemory 交付大对象。**

可信短任务可以进程内执行，但必须明确它与隔离 runner 的保证不同。

**Android 真正要求改变的是 runner 的启动、承载和生命周期适配，不是放弃进程隔离。一个安装包、少量按需进程、统一核心规则，比“所有东西塞进单进程”或“每个插件永久一个进程”都更适合 Morrow。**