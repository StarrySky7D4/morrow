name: um_decrypt
description: Pure decryption using the same Rust core through native FFI or Web WASM.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.4.0 <4.0.0'
dev_dependencies:
  test: ^1.29.0
