# Morrow 0.1.7+8 — 2026-09-09

Complete the daemon → Morrow rename across product labels, Dart imports, Windows metadata, Web metadata, platform channels, documentation and distribution filenames. The local project directory is now `morrow` and path-dependent build caches were regenerated after the move.

Windows imports legacy preferences without deleting the source or overwriting existing Morrow preferences. Persisted preference keys, the Web media database and Android installation IDs remain stable. Existing media references continue to point to their original files.

Validation from the renamed directory:

- `flutter pub get --offline`: passed, without dependency upgrades.
- `flutter analyze --no-pub`: no issues.
- `flutter test --no-pub`: all 36 tests passed, including storage migration and Android layout tests.
- `flutter build windows --release --no-pub`: passed; executable metadata is `dev.morrow`, `morrow_studio.exe`, version `0.1.7+8`.
- `flutter build web --release --no-pub --no-web-resources-cdn`: passed; Web version metadata is `0.1.7`, build `8`, package `morrow_studio`.
- PowerShell scripts: syntax parsing passed.
- Every archive file matches its staging file by SHA-256. Both archives include the exact root LICENSE and NOTICE; no QA/demo directories or old daemon executable are packaged.

| Artifact | Bytes | Files | SHA-256 |
| --- | ---: | ---: | --- |
| `morrow-0.1.7-windows-x64.zip` | 35,530,304 | 52 | `6f4925509fc27c5dbc356577e31bd8f37957660ddc9a074215b8bf96c282884c` |
| `morrow-0.1.7-web.zip` | 14,838,209 | 53 | `a92ac3a6f73d87a161e8cc9b0b1e3124d91b2fbbc262863cfb45f1945d5fb354` |

Verification limits: the Windows plugin emits upstream CMake CMP0175 developer warnings; the Web build emits the existing CupertinoIcons font warning. Neither failed the build. Wasm dry-run passed, but no Wasm distribution or runtime verification is claimed. Windows live-window integration and manual media interactions were not repeated for this release.

The source includes Android project, file handling, layout and SDK setup preparations. The local Android SDK is incomplete, so no Android APK compilation or device validation is claimed and no APK is included in this release.
