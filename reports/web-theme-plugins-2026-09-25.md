# Web 主题插件支持与验收

## 本次范围

仅开放符合现有 `theme.describe` 类型协议、无业务权限、无 IO/服务声明、无依赖的主题包。完整通用插件方案保留为后续工作；本次不增加浏览器文件系统授权、网络、凭据或服务运行能力。

改动在 `codex/web-theme-plugins` 隔离工作区整合。主工作区其他未提交的菜单、提示、卡片编辑等功能未纳入发布，也未覆盖。

## 实现

- 新增 `ThemePackageImportControl`，Web 使用选中文件的有界不可变快照，检查和确认复用同一个 Blob；Windows 仍使用原路径接口。
- Worker 接收小型控制帧和独立 Blob，Rust 以最多 64 KiB 分块读取。包上限沿用 `MAX_PACKAGE_BYTES = 4,276,930`；普通请求仍限 128 KiB。
- 宿主校验归档、主题协议、权限及依赖。主题专用通道和原有 Web 字节导入入口均执行限制；确认绑定包摘要和登记修订号。
- 安装、批准、启停、升级和卸载复用现有 OPFS 登记库。新导入默认停用；同版本同摘要可重复检查，过期修订和摘要不匹配拒绝。
- 通用主题控制器加载配色、描述和分块插画，校验长度、SHA-256、声明尺寸和实际解码尺寸。修复 Flutter Web 不支持 `ImageDescriptor.width/height` 的问题：先读取编码图片尺寸，再核对解码帧尺寸。
- 同一资料库只启用一个主题，切换时保留业务插件批准。主题覆盖属于显示层；停用/卸载恢复原有背景、音乐和设置。
- 错误文件返回普通失败回执，不关闭 Worker。导入回执未确认时不自动重放或启用，重开读取设备登记并提示用户核对。
- 包、登记、启用状态及主题偏好留在设备侧。GitHub Pages 仅分发静态程序。

## 原始包

验收直接选择 `test/fixtures/plugins/morrow-mid-autumn-1.0.0.morrowplugin`，它与用户指定的 `dist/plugins/morrow-mid-autumn-1.0.0.morrowplugin` 字节一致，未重新打包或变更版本。

- 大小：661,049 字节。
- SHA-256：`b9c8dd591ef88e2d14be4ee7f4b402ff5d6563530618f71cbaf1e4d1b61d9751`。
- ID：`org.morrow.theme.mid-autumn`；版本：`1.0.0`。
- 宿主和客户端均未按此 ID 或名称特判。

## 验证

- 宿主：原始大包、默认停用、摘要绑定、过期预览、重复导入、重开恢复、损坏/超限/非主题包及带权限/IO/依赖的伪主题拒绝。
- 登记与运行时：主题互斥原子切换、旧主题撤销、业务插件启用与批准不变。
- Windows：真实原始主题路径导入、启用、插画、关闭重开、停用和卸载；内置工作台摘要/批准/启用状态不变，业务歌词解析可用。
- Flutter：主题切换、失效描述/插画/尺寸/修订、设置保存失败、窄屏、浅深色、编辑草稿和原外观保留。
- 真实浏览器：实际文件选择、检查/取消、错误文件拒绝、提交后丢失回执、重开后保持停用、明确启用、浅深色截图、刷新与关闭标签后新开恢复、停用/卸载，以及继续保存内容和媒体。
- 现有 Pages 内容、离线编辑、旧资料、孤立数据保护和图片/视频/音乐保存场景继续验收。
- 发布门禁新增主题宿主/界面回归及 CPU 4 倍限速主题浏览器验收。自动化等待实际 Worker 回执与界面状态，避免用固定短暂停代替确认。

## 发布结果

正式网址：https://starrysky7d4.github.io/morrow/ 。已部署应用源提交为 `52f7345fb331b6101082e0bf8862020faf720537`。

[GitHub Actions 36112225032](https://github.com/StarrySky7D4/morrow/actions/runs/36112225032) 的 build 与 deploy 均成功。主题宿主及界面回归、fresh/offline、legacy、orphan、media、theme/CPU 4 倍限速全部通过。

正式网址复验：Chrome `154.0.8037.57` 与 Edge `153.0.4234.48` 均完成原始文件选择、取消/拒绝、导入中断恢复、明确启用、浅深色显示、刷新与关闭标签后恢复、停用、卸载，并继续保存内容和背景音乐。浅深色截图已人工检查。

`verify_deployed_web.py --commit 52f7345fb331b6101082e0bf8862020faf720537` 通过 HTTPS、源版本、基础路径、MIME 和 36 项产物 SHA-256 校验。正式内置工作台包仍为 872,568 字节，SHA-256 为 `b28aea5f6989967d33b6c2797624a64068da0b4d1aadc57b8db1c74cc4a79985`，与此前部署完全一致。

Chrome 成功复验记录 285 次 HTTP 请求、0 次上传；增强后的 Edge 记录包含 Worker 资源，共 341 次 HTTP 请求、0 次上传。请求为静态程序、素材和 Flutter 备用字体读取；未出现携带内容、媒体或主题包的网络请求。Worker 代码也仅新增本机 Blob 通道。

本地回归结果：Flutter/真实 Windows 宿主共 32 项；插件目录 10 项、主题导入 2 项、登记库 11 项、运行时管理 11 项均通过。主题相关 Dart 分析无问题。

验证过程曾遇到一轮 CI 主题场景未结束，取消后补充退出清理、阶段日志和分组时限，最终上述运行通过。Edge 线上尝试也曾出现初始化和截图超时；同一应用产物最终完成全流程。后续诊断脚本保留原始错误、记录 Worker 网络请求、延长线上加载及截图等待，并在重建页面/Worker 时保留浏览器静态代码缓存。所有功能和持久化断言均保留；这些诊断改动不改变已发布应用。

本地完整证据位于隔离工作区 `build/theme-pages-http.json`、`build/theme-live-chrome.log`、`build/theme-live-edge-worker.log`、`build/theme-live-*-network.json` 和 `build/theme-live-*-light.png` / `build/theme-live-*-dark.png`。

## 后续完整方案（本次不执行）

通用外部插件入口、全类型包预检与能力协商、设备文件系统适配、网络与跨域策略、凭据管理、服务生命周期和相应授权/恢复验收，继续作为独立后续范围。当前支持主题包不代表通用外部插件已经兼容 Web。
