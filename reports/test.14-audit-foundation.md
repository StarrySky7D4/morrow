# test.14 审计底座前置验证

日期：2026-09-12。独立 `morrow-audit` 模块版本 test.14；应用继续使用 test.13。未推送、未发布 Release、未清空或确认用户数据库的待封存队列。

本轮提供：原始 Protobuf 字节 Ed25519 签名、LZ4 分段容器、固定可信日志与公钥核验、链顺序与前段摘要检查、指定检查点的历史截断拒绝、独立只读验证器。

验证结果：

- 三组 Rust 测试通过，覆盖原始提交与未知字段保留、解析重编码后的签名拒绝、错误公钥、签名篡改、乱序／重复段、检查点截断、事件身份与容量边界。
- 严格 Clippy 通过。
- 真实 Release 生成器使用新的合成数据库产生两段、三条提交，原待封存队列仍保留三条事件。
- 真实 Release 验证器接受完整链与检查点；只提供第一段时退出非零，记录 `Error: Checkpoint`。
- Wasm 库 `wasm32-unknown-unknown` 编译通过，不代表浏览器运行或生产密钥管理完成。

证据目录：`build/audit/qualification-90e2cda0f2ff495291337f1ca78de567`。其中所有签名使用公开的确定性测试密钥，仅适用于合成数据；不得用于用户数据库或生产信任。

最终验证器：`build/audit/release/morrow-audit-check.exe`。
SHA-256：`cf23edadd9c3a9f52043b9430d760dfb00f3e72116feb7497a9965bbb2b62c4a`。
最终输出明确说明，提供检查点不等于证明它独立保存，不自动构成外部见证。

仍待完成：生产密钥的创建／凭据保存／轮换、宿主授权及调用等完整审计事实、数据库归档与封存回执的原子提交、崩溃恢复、幂等队列确认、检查点交换和隔离重放。因此本轮不能解除 1024 条／64 MiB 待封存队列限制，也不标记 M4 完成。

复验：`tool/verify_audit.ps1`。契约与接入顺序见 [审计模块](../audit/README.md)。
