# test.12 配置容量与 Windows 验证

日期：2026-09-12。应用版本 `0.1.9-test.12+21`，工作台插件和宿主 test.12；核心与三语言 SDK 仍为 test.11 / 运行期 v7。

本轮将整份偏好从 64 KiB 提升到 4 MiB，32 KiB 分段传输并核对完整摘要，插件按页校验后执行单次原子保存。单首歌曲整体仍有 64 KiB 页限制，歌词字段仍限 48 KiB；通用 SDK 流式写入尚未实现。

验证通过：

- 工作台插件 11 项测试，含 80 首歌曲、超过 3 MiB 配置往返、总量与单条边界。
- 宿主 4 项测试，含乱序、错误标识、重复、不完整、校验失败、固定超时与取消，以及原有持久化和分页集成。
- Flutter 全套 73 项测试；真实 Rust Wasm 集成覆盖 24 首歌曲歌词保存、调用方缓冲区重用、连续写入、末条内容非法时保留原设置、重启恢复。
- Flutter 静态检查、宿主严格 Clippy、生成协议核对与差异格式检查通过。
- 最终包中的 Windows Release 实际启动，退出码 0。原生磨砂接口、实际静音 WAV 解码时钟、跳转和音频互斥、无自动播放、Flutter 渲染检查通过。尚未做窗口后方桌面像素对照。

交付包：`dist/morrow-0.1.9-test.12-rust-workbench-windows.zip`，39,365,895 字节。包内 236 个文件已逐个核对 SHA256SUMS。

SHA-256：`d8f4dca06d4be3514b5b29817f995d6c39aac8734e8b2ca41431f2a50268cc14`。

实际运行证据：`build/workbench-host/windows-test12-bac268a2127546dcb5a17d4b6681c80b/result.md` 与 `result.png`。使用独立合成数据目录，没有覆盖原用户数据。未推送，未发布 Release。

后续仍需通用大正文写入、事件确认与容量回收、草稿恢复、集合与附件 API、安装升级生命周期及其他平台验收。参见 [功能对照与待办](../docs/TEST1_RUST_PARITY.md)。
